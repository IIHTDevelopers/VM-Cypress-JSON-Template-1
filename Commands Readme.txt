* Before executing any command, please execute a batch file with name "runner" present inside Cypress folder. This will keep pushing the code in background at regular interval.

Commands:
* Install all dependencies in the Cypress folder path using:
	npm install

* Run the following command to open the interactive Cypress Test Runner:
	npx cypress open

* Run the Tests in the Cypress folder path:
	npx cypress run
